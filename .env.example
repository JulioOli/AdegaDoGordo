# Google Analytics 4 — Measurement ID (Configurações do projeto Firebase → Integrações → Analytics)
# Copie este arquivo para .env.local e preencha o ID antes do deploy:
#   cp .env.example .env.local
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
